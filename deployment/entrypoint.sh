#!/bin/sh
cd crawl/crawl
# poetry install 
# poetry add logparser
# set -m
# scrapyd
# poetry run logparser -dir /scrapyd/logs &
# cd scrapers/olx
# cat scrapy.cfg
scrapyd-deploy docker
# cd ../otodom
# scrapyd-deploy docker
# fg scrapyd

