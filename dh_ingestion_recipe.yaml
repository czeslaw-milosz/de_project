source:
    type: delta-lake
    config:
        platform_instance: my-delta-lake
        base_path: 's3://housing'
        s3:
            aws_config:
                aws_access_key_id: admin
                aws_secret_access_key: adminadmin
                aws_endpoint_url: 'http://minio:9000'
                aws_region: us-east-1
